[package]
name = "q-buffers"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
q-parity = { path = "../q-parity" }

[lints]
workspace = true

[[bin]]
name = "bench-bar-geometry"
path = "src/bin/bench_bar_geometry.rs"
